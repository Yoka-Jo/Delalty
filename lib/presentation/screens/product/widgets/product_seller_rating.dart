part of 'widgets.dart';

class ProductSellerRating extends StatelessWidget {
  const ProductSellerRating({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const UserRating(rating: 5);
  }
}
